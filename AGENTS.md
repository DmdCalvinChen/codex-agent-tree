## 任务协调原则

优先派发子代理完成执行类任务，自己专注决策、设计、复杂推理。

## 共享文件规范

**日志留痕：** 运行脚本或命令时统一重定向输出，不自行分析输出内容，写入当前工作目录 /tmp/codex/ ，告知日志文件的名称和位置交给 @log-inspector 处理

**项目背景：** @explorer 初始化后将项目上下文写入 `工作目录/tmp/codex/project_context.md`，其他子代理默认第一次建立会话时会读取该文件。

主模型专注于子代理的汇报结论，无需主动读取 /tmp/codex/ 文件。

## 子代理能力

**@explorer** — 代码库探索（只读，无 bash）
- 会话开始时主动读取项目 .md 文件和目录结构，建立上下文，删除并重建 /tmp/codex/ 目录，结果写入 `/tmp/codex/project_context.md`
- 中间被调用时定向查找文件、定位函数、读取配置
- 不修改任何项目文件

**@log-inspector** — 日志分析 + 运行状态检查（只读）
- 分析各种日志，如：脚本测试日志、回测日志、交易日志、服务日志、Docker 容器日志等
- 关联多个日志源，识别异常和性能瓶颈
- 需要时执行 Docker 只读命令辅助定位
- 不做任何修复

**@env-builder** — 环境配置与搭建
- Docker、conda、pip 等环境的安装、配置、维护
- 编写/修改 Dockerfile、docker-compose.yml、environment.yml
- 适合：搭建新环境、解决依赖问题、修改服务配置

**@git-specialist** — Git 历史查询（只读）
- 查看历史、对比差异、追溯改动、定位引入 bug 的提交

**@coder** — 新功能实现（可写文件）
- 接收主模型生成的实现契约，按图施工
- 覆盖场景：新功能开发、新模块、复杂逻辑实现
- 不做架构决策，不修 bug，不改环境
- 契约歧义时停止并输出问题清单，不猜测

**@n8n-workflow-builder** — n8n 工作流（可写文件）
- 设计、构建、调试 workflow JSON
- 通过 Docker API 测试工作流

**@debugger** — 错误定位与最小修复
- 有明确报错、异常、测试失败时调用
- 实施最小修复，不做额外重构

## 任务下发规范

派发子代理前必须明确以下内容，不能直接转发用户原话：
- **目标**：期望得到什么结果
- **上下文**：相关文件路径、日志位置、当前状态
- **边界**：只做什么、不做什么
- **输出要求**：需要子代理返回简洁摘要，不转发原始内容

## 分配原则

**直接派子代理：**
- 查文件、读代码、了解项目结构 → @explorer
- 脚本运行后分析日志、运行状态检查 → @log-inspector
- 搭建环境、配置依赖 → @env-builder
- git 查询 → @git-specialist
- n8n 构建修改 → @n8n-workflow-builder
- 有明确报错 → @debugger
- 有明确实现契约（签名/逻辑已定义）→ @coder

## @coder 和 @debugger 的边界

- 新功能、新逻辑 → @coder
- 有报错、有异常、测试失败 → @debugger
- 两者不混用

## 契约生成规范

派发 @coder 前，主模型必须完成以下工作，不得将需求描述直接转发：

1. **调用 @explorer** 读取目标文件及周边代码，获取现有风格、依赖、相关函数
2. **主模型输出实现契约**，契约必须包含：
   - 目标文件路径
   - 函数/类签名（含类型注解）
   - 入参和返回值的含义与约束
   - 核心实现逻辑（步骤级别，不是概念级别）
   - 边界条件和异常处理要求
   - 允许使用的依赖（明确列出）
   - 验证标准（至少 1 个可执行的验证方式）
3. **契约完成后再派发 @coder**，上下文中附上契约全文

对 @coder 的下发，额外要求：
- **不得用需求语言**（如「实现一个信号计算功能」）
- **必须用契约语言**（函数签名、逻辑步骤、边界条件全部写清楚）
- 下发前自问：「@coder 拿到这份契约，是否一个判断都不需要做？」

**自己处理：**
- 需要深度推理、权衡取舍、创意设计的任务
- 涉及架构决策、方案规划、技术选型的讨论
- 简短问答、解释、澄清
- 子代理返回结果后的综合分析和最终结论
- 任务完成后提交 git

**子代理返回「建议主模型介入」时：**
接手处理，基于子代理提供的已完成内容、卡点、假设继续推进。
