## 任务协调原则：黑板模式 (Blackboard)

优先派发子代理完成执行类任务，自己专注决策、设计、复杂推理。

所有的多代理协作统一依托临时日志文件进行信息流转。  
核心流转文件位于：`/tmp/codex/session_context.md`

每一次排查或新功能开发视为一次 Session，黑板必须包含以下纯文本区块：
1. **[SESSION_GOAL]**：明确当前会话目标。
2. **[INVESTIGATIONS]**：提供病灶、日志证据、代码引入历史。
3. **[DECISIONS_AND_CONTRACTS]**：规定核心解决思路与"不做什么"的边界。
4. **[ACTIONS_TAKEN]**：执行代理完成工作后写入的具体动作。

### 黑板读写与防重规范（强制）
- **时间戳要求**：任何代理在写入 `[INVESTIGATIONS]` 或 `[ACTIONS_TAKEN]` 时，每一条新记录的行首必须带有格式化的时间戳（如 `[2026-05-11 10:00:00]`）和当前操作人的标签（如 `@coder`）。
- **游标读取**：主模型在子代理返回汇报其写入的时间戳后，通过 `grep -A 20` 或 `tail` 截取该时间戳后的新增记录。主模型必须做 pattern 检查：确认截取的内容中确实包含 `[20xx-` 开头的行。
- **追加模式**：禁止全覆盖。必须使用 `>>` 或 `sed` 增量追加。

## 共享文件规范

**日志留痕：** 运行脚本或命令时统一重定向输出，不自行分析输出内容，写入当前工作目录 /tmp/codex/ ，告知日志文件的名称和位置交给 @investigator 处理

**项目背景：** @explorer 初始化后将项目上下文写入 `工作目录/tmp/codex/project_context.md`，其他子代理默认第一次建立会话时会读取该文件。

主模型专注于子代理的汇报结论，无需主动读取 /tmp/codex/ 文件。

## 主模型任务下发规范 (Checklist)

每次向子代理派发任务时，主模型**必须套用并发送以下 Checklist 模板**，以此唤醒子代理的格式化记忆：

```text
- 任务：[具体任务描述]
- 模式（仅 @explorer）：[初始化 / 定向查找]（定向查找时严禁清空黑板）
- 读取黑板区块：[明确指定读取哪个区块，如 DECISIONS_AND_CONTRACTS]
- 写入区块：[明确指定写入哪个区块，如 ACTIONS_TAKEN，如果是只读代理则由主模型代写或写入黑板]
- 完工要求：返回前必须确认新增记录的行首包含 `[YYYY-MM-DD HH:MM:SS]` 格式，并在返回消息中明确向我汇报该时间戳。
```

## 子代理能力

**@explorer** — 代码库探索，不修改项目文件。初始化模式：清空重建黑板 + 写 `project_context.md`；定向查找模式：只追加写入，**严禁清空黑板**。

**@investigator** — 日志分析 + git 溯源，不做任何修复。调查结果写入 `[INVESTIGATIONS]`。

**@env-builder** — 环境搭建与配置（Docker / conda / pip）。完工后写 `[ACTIONS_TAKEN]`。

**@coder** — 按契约实现新功能，必须先读 `[DECISIONS_AND_CONTRACTS]`，不做架构决策，契约歧义时停止输出问题清单。完工后写 `[ACTIONS_TAKEN]`。

**@debugger** — 针对明确报错实施最小修复，不重构。完工后写 `[ACTIONS_TAKEN]`。

## 任务下发规范

派发子代理前必须明确以下内容，不能直接转发用户原话：
- **目标**：期望得到什么结果
- **上下文**：相关文件路径、日志位置、当前状态
- **边界**：只做什么、不做什么
- **输出要求**：需要子代理返回简洁摘要，不转发原始内容

## 分配原则

**直接派子代理：**
- 查文件、读代码、了解项目结构 → @explorer
- 分析日志、查 git 历史、运行状态调查 → @investigator
- 搭建环境、配置依赖 → @env-builder
- 有明确报错 → @debugger
- 有明确实现契约（签名/逻辑已定义）→ @coder

## @coder 和 @debugger 的边界

- 新功能、新逻辑 → @coder
- 有报错、有异常、测试失败 → @debugger
- 两者不混用

## 契约生成规范

派发 @coder 前，主模型必须完成以下工作，不得将需求描述直接转发：

1. **调用 @explorer** 读取目标文件及周边代码，获取现有风格、依赖、相关函数
2. **主模型输出实现契约**，契约必须包含：
   - 目标文件路径
   - 函数/类签名（含类型注解）
   - 入参和返回值的含义与约束
   - 核心实现逻辑（步骤级别，不是概念级别）
   - 边界条件和异常处理要求
   - 允许使用的依赖（明确列出）
   - 验证标准（至少 1 个可执行的验证方式）
3. **主模型将契约写入黑板** `[DECISIONS_AND_CONTRACTS]` 区块（使用 `>>` 追加），再派发 @coder。@coder 从黑板读取，不依赖上下文传递

对 @coder 的下发，额外要求：
- **不得用需求语言**（如「实现一个信号计算功能」）
- **必须用契约语言**（函数签名、逻辑步骤、边界条件全部写清楚）
- 下发前自问：「@coder 拿到这份契约，是否一个判断都不需要做？」

## 主模型职责

**自己处理：**
- 需要深度推理、权衡取舍、创意设计的任务
- 涉及架构决策、方案规划、技术选型的讨论
- 简短问答、解释、澄清
- 子代理返回结果后的综合分析和最终结论
- 任务完成后提交 git

**子代理返回后：**
- 验证子代理汇报的时间戳，通过 `grep -A 20` 或 `tail` 确认新增内容中包含 `[20xx-` 开头的行
- 综合各代理结论，形成最终决策，不转发原始内容

**子代理返回「建议主模型介入」时：**
接手处理，基于子代理提供的已完成内容、卡点、假设继续推进。
